# Verified environment (conda env `Lider_5090`, Windows 11 + RTX 5090 / sm_120)
# Python 3.10.20
#
# PyTorch must match your GPU + CUDA. The versions below are cu128 builds needed for
# Blackwell (sm_120). On an older GPU (e.g. 3090/4090) any torch >= 2.1 with a matching
# CUDA build works -- install it from https://pytorch.org first, THEN `pip install -r`
# the rest of this file.
#
#   pip install torch==2.11.0 torchvision==0.26.0 --index-url https://download.pytorch.org/whl/cu128

torch==2.11.0
torchvision==0.26.0
timm==1.0.26
numpy==2.2.6
pandas==2.3.3
pillow==12.2.0
huggingface_hub==1.17.0
safetensors==0.7.0
scipy==1.15.3
